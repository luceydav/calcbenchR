#' Calcbench API
#'
#' No description provided (generated by Openapi Generator https://github.com/calcbenchRtools/calcbenchR-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://calcbenchR-generator.tech
#'
#' @docType class
#' @title RawXBRL operations
#' @description RawXBRLApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ RawXBRLXBRLTagValues } \emph{ Get data for individual tags on the multi-company page. }
#' See <a href=\"https://www.calcbench.com/multi?pg_classificationMethod=index&amp;pg_title=Dow\%20Jones\%20Industrial\%20Average&amp;pg_sectorCode=DJIA&amp;mc_metrics=\%7B\%22metric\%22:\%22Revenues\%22,\%22dataSet\%22:2,\%22format_type\%22:\%22currency\%22\%7D\">Example</a>
#'
#' \itemize{
#' \item \emph{ @param } search_parameters \link{XBRLTagSearchParameters}
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[object]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  RawXBRLXBRLTagValues  ####################
#'
#' library(calcbenchR)
#' var_search_parameters <- XBRLTagSearchParameters$new(XBRLTagQueryParameters$new(c("tags_example"), c("metrics_example")), CompaniesParameters$new(c("companyIdentifiers_example"), "entireUniverse_example", c(123)), PeriodParameters$new(123, "unset", 123, "unset", "periodType_example", "useFiscalPeriod_example", DateRange$new("startDate_example", "endDate_example"), "allHistory_example", "updateDate_example", "updatedFrom_example", "asOriginallyReported_example", 123, 123), MetricSearchCriteria$new(c("metrics_example"), c("company_identifiers_example"), 123, 123, 123, 123, "fiscal_period_example", 123, "calendar_period_example", 123, "include_trace_example", "entire_universe_example", 123, "use_fiscal_periods_example", "as_originally_reported_example", "all_history_example", "PointInTime_example", "updateDate_example", "unset", "Annual_example", "Quarterly_example", c("unset"), c(key = "inner_example"))) # XBRLTagSearchParameters | 
#'
#' #Get data for individual tags on the multi-company page.
#' api_instance <- RawXBRLApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RawXBRLXBRLTagValues(var_search_parametersdata_file = "result.txt")
#' result <- api_instance$RawXBRLXBRLTagValues(var_search_parameters)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
RawXBRLApi <- R6::R6Class(
  "RawXBRLApi",
  public = list(
    api_client = NULL,
    #' Initialize a new RawXBRLApi.
    #'
    #' @description
    #' Initialize a new RawXBRLApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get data for individual tags on the multi-company page.
    #'
    #' @description
    #' Get data for individual tags on the multi-company page.
    #'
    #' @param search_parameters 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[object]
    #' @export
    RawXBRLXBRLTagValues = function(search_parameters, data_file = NULL, ...) {
      local_var_response <- self$RawXBRLXBRLTagValuesWithHttpInfo(search_parameters, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get data for individual tags on the multi-company page.
    #'
    #' @description
    #' Get data for individual tags on the multi-company page.
    #'
    #' @param search_parameters 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[object]) with additional information such as HTTP status code, headers
    #' @export
    RawXBRLXBRLTagValuesWithHttpInfo = function(search_parameters, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`search_parameters`)) {
        stop("Missing required parameter `search_parameters`.")
      }


      if (!is.null(`search_parameters`)) {
        local_var_body <- `search_parameters`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/XBRLTagValuesV2"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "text/json", "text/html", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "text/json", "text/html", "application/x-www-form-urlencoded")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[object]", loadNamespace("calcbenchR")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
