#' Calcbench API
#'
#' No description provided (generated by Openapi Generator https://github.com/calcbenchRtools/calcbenchR-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://calcbenchR-generator.tech
#'
#' @docType class
#' @title StandardizedData operations
#' @description StandardizedDataApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ StandardizedDataMappedData } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } search_parameters \link{MappedDataSearchParameters}
#' \item \emph{ @returnType } list( \link{MappedDataPoint} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[MappedDataPoint]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  StandardizedDataMappedData  ####################
#'
#' library(calcbenchR)
#' var_search_parameters <- MappedDataSearchParameters$new(StandardizedQueryParameters$new("pointInTime_example", "pointInTimeV2_example", "allFootnotes_example", "allFace_example", "includeXBRL_example", "includePreliminary_example", "allNonGAAP_example", "allMetrics_example", "excludeUnconfirmedPreliminary_example", c("metrics_example"), "includeTrace_example", "excludeErrors_example"), PeriodParameters$new(123, "unset", 123, "unset", "periodType_example", "useFiscalPeriod_example", DateRange$new("startDate_example", "endDate_example"), "allHistory_example", "updateDate_example", "updatedFrom_example", "asOriginallyReported_example", 123, 123), CompaniesParameters$new(c("companyIdentifiers_example"), "entireUniverse_example", c(123))) # MappedDataSearchParameters | 
#'
#' api_instance <- StandardizedDataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$StandardizedDataMappedData(var_search_parametersdata_file = "result.txt")
#' result <- api_instance$StandardizedDataMappedData(var_search_parameters)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
StandardizedDataApi <- R6::R6Class(
  "StandardizedDataApi",
  public = list(
    api_client = NULL,
    #' Initialize a new StandardizedDataApi.
    #'
    #' @description
    #' Initialize a new StandardizedDataApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param search_parameters 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[MappedDataPoint]
    #' @export
    StandardizedDataMappedData = function(search_parameters, data_file = NULL, ...) {
      local_var_response <- self$StandardizedDataMappedDataWithHttpInfo(search_parameters, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param search_parameters 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[MappedDataPoint]) with additional information such as HTTP status code, headers
    #' @export
    StandardizedDataMappedDataWithHttpInfo = function(search_parameters, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`search_parameters`)) {
        stop("Missing required parameter `search_parameters`.")
      }


      if (!is.null(`search_parameters`)) {
        local_var_body <- `search_parameters`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/mappedData"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "text/json", "text/html", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "text/json", "text/html", "application/x-www-form-urlencoded")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[MappedDataPoint]", loadNamespace("calcbenchR")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
