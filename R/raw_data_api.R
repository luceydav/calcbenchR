#' Calcbench API
#'
#' No description provided (generated by Openapi Generator https://github.com/calcbenchRtools/calcbenchR-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://calcbenchR-generator.tech
#'
#' @docType class
#' @title RawData operations
#' @description RawDataApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ RawDataRaw13FData } \emph{ 13F data }
#' The data behind the <a href=\"/xbrl\">13F data page.</a>
#'
#' \itemize{
#' \item \emph{ @param } params \link{Raw13FDataSearchParams}
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : object
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ RawDataRawNonXBRLData } \emph{ Non-XBRL Data As-Reported }
#' The data behind the <a href=\"/nonXBRLRawData\">non-XBRL raw data</a> page.
#'
#' \itemize{
#' \item \emph{ @param } params \link{RawNonXBRLDataSearchParams}
#' \item \emph{ @returnType } list( \link{NonXBRLFact} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[NonXBRLFact]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ RawDataRawXBRLData } \emph{ XBRL data As-Reported }
#' The data behind the <a href=\"/xbrl\">raw data page.</a>
#'
#' \itemize{
#' \item \emph{ @param } params \link{RawXBRLDataSearchParams}
#' \item \emph{ @returnType } list( \link{RawXBRLFact} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[RawXBRLFact]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  RawDataRaw13FData  ####################
#'
#' library(calcbenchR)
#' var_params <- Raw13FDataSearchParams$new(CompaniesParameters$new(c("companyIdentifiers_example"), "entireUniverse_example", c(123)), Raw13FQueryParams$new(c(Form13FQueryPiece$new("fiscalYear", 123, "value_example")), "QueryID_example")) # Raw13FDataSearchParams | 
#'
#' #13F data
#' api_instance <- RawDataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RawDataRaw13FData(var_paramsdata_file = "result.txt")
#' result <- api_instance$RawDataRaw13FData(var_params)
#' dput(result)
#'
#'
#' ####################  RawDataRawNonXBRLData  ####################
#'
#' library(calcbenchR)
#' var_params <- RawNonXBRLDataSearchParams$new(CompaniesParameters$new(c("companyIdentifiers_example"), "entireUniverse_example", c(123)), RawNonXBRLQueryParams$new(c(NONxbrlQueryPiece$new("numericalValue", "equals", "value_example")), "QueryID_example")) # RawNonXBRLDataSearchParams | 
#'
#' #Non-XBRL Data As-Reported
#' api_instance <- RawDataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RawDataRawNonXBRLData(var_paramsdata_file = "result.txt")
#' result <- api_instance$RawDataRawNonXBRLData(var_params)
#' dput(result)
#'
#'
#' ####################  RawDataRawXBRLData  ####################
#'
#' library(calcbenchR)
#' var_params <- RawXBRLDataSearchParams$new(RawXBRLQueryParams$new(c(XbrlQueryPiece$new("numericalValue", "equals", "value_example")), "QueryID_example"), CompaniesParameters$new(c("companyIdentifiers_example"), "entireUniverse_example", c(123))) # RawXBRLDataSearchParams | 
#'
#' #XBRL data As-Reported
#' api_instance <- RawDataApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$RawDataRawXBRLData(var_paramsdata_file = "result.txt")
#' result <- api_instance$RawDataRawXBRLData(var_params)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
RawDataApi <- R6::R6Class(
  "RawDataApi",
  public = list(
    api_client = NULL,
    #' Initialize a new RawDataApi.
    #'
    #' @description
    #' Initialize a new RawDataApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 13F data
    #'
    #' @description
    #' 13F data
    #'
    #' @param params 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return object
    #' @export
    RawDataRaw13FData = function(params, data_file = NULL, ...) {
      local_var_response <- self$RawDataRaw13FDataWithHttpInfo(params, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 13F data
    #'
    #' @description
    #' 13F data
    #'
    #' @param params 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (object) with additional information such as HTTP status code, headers
    #' @export
    RawDataRaw13FDataWithHttpInfo = function(params, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`params`)) {
        stop("Missing required parameter `params`.")
      }


      if (!is.null(`params`)) {
        local_var_body <- `params`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/raw13FData"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "text/json", "text/html", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "text/json", "text/html", "application/x-www-form-urlencoded")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "object", loadNamespace("calcbenchR")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Non-XBRL Data As-Reported
    #'
    #' @description
    #' Non-XBRL Data As-Reported
    #'
    #' @param params 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[NonXBRLFact]
    #' @export
    RawDataRawNonXBRLData = function(params, data_file = NULL, ...) {
      local_var_response <- self$RawDataRawNonXBRLDataWithHttpInfo(params, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Non-XBRL Data As-Reported
    #'
    #' @description
    #' Non-XBRL Data As-Reported
    #'
    #' @param params 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[NonXBRLFact]) with additional information such as HTTP status code, headers
    #' @export
    RawDataRawNonXBRLDataWithHttpInfo = function(params, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`params`)) {
        stop("Missing required parameter `params`.")
      }


      if (!is.null(`params`)) {
        local_var_body <- `params`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/rawNonXBRLData"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "text/json", "text/html", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "text/json", "text/html", "application/x-www-form-urlencoded")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[NonXBRLFact]", loadNamespace("calcbenchR")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' XBRL data As-Reported
    #'
    #' @description
    #' XBRL data As-Reported
    #'
    #' @param params 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[RawXBRLFact]
    #' @export
    RawDataRawXBRLData = function(params, data_file = NULL, ...) {
      local_var_response <- self$RawDataRawXBRLDataWithHttpInfo(params, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' XBRL data As-Reported
    #'
    #' @description
    #' XBRL data As-Reported
    #'
    #' @param params 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[RawXBRLFact]) with additional information such as HTTP status code, headers
    #' @export
    RawDataRawXBRLDataWithHttpInfo = function(params, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`params`)) {
        stop("Missing required parameter `params`.")
      }


      if (!is.null(`params`)) {
        local_var_body <- `params`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/rawXBRLData"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "text/json", "text/html", "text/csv")

      # The Content-Type representation header
      local_var_content_types <- list("application/json", "text/json", "text/html", "application/x-www-form-urlencoded")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[RawXBRLFact]", loadNamespace("calcbenchR")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
