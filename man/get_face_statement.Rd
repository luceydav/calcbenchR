% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_face_statement.R
\name{get_face_statement}
\alias{get_face_statement}
\title{Get As-Reported Face Statement}
\usage{
get_face_statement(
  company,
  statement_type = "income",
  period_type = "annual",
  all_history = FALSE,
  descending_dates = FALSE,
  currency = NULL,
  include_non_xbrl = FALSE
)
}
\arguments{
\item{company}{character; single ticker or CIK code, e.g. \code{"AAPL"}}

\item{statement_type}{character; one of \code{"income"}, \code{"balance"},
\code{"cash"}, \code{"change-in-equity"}, or
\code{"comprehensive-income"} (default \code{"income"})}

\item{period_type}{character; one of \code{"annual"}, \code{"quarterly"},
\code{"combined"}, or \code{"cumulative"} (default \code{"annual"})}

\item{all_history}{logical; if \code{TRUE} return all available periods
(default \code{FALSE})}

\item{descending_dates}{logical; if \code{TRUE} sort most recent period
first (default \code{FALSE})}

\item{currency}{character; currency code for conversion, e.g. \code{"USD"}
(default \code{NULL} uses the company's reporting currency)}

\item{include_non_xbrl}{logical; if \code{TRUE} include data from earnings
releases in addition to SEC filings (default \code{FALSE})}
}
\value{
An \code{APIXBRLStatement} R6 object containing:
\describe{
\item{\code{entity_name}}{Company name}
\item{\code{name}}{Statement name}
\item{\code{period_type}}{Period type}
\item{\code{columns}}{List of \code{FaceStatementColumn} objects
(each with \code{fiscal_period}, \code{period_start},
\code{period_end}, etc.)}
\item{\code{line_items}}{List of line item objects with values
indexed by column}
}
Use \code{result$columns} and \code{result$line_items} to access the data.
}
\description{
Retrieve an as-reported financial statement (income statement,
balance sheet, cash flow, etc.) for a single company from Calcbench.
}
